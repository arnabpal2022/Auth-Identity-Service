meta {
  name: RefreshToken
  type: http
  seq: 8
}

post {
  url: http://localhost:8080/api/v1/auth/refresh
  body: json
  auth: inherit
}

body:json {
  {
    "refreshToken" : ""
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
